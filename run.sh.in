#!/bin/sh
export LD_LIBRARY_PATH=@INSTALL_PREFIX@
export POGONA_SHADER_DIR=@INSTALL_PREFIX@/shaders # this is handy because it does not require you to be inside the INSTALL_PREFIX to run this script
@INSTALL_PREFIX@/runner $@
